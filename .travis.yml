language: python

python:
  - 3.3
  - 3.4
  - nightly

matrix:
  allow_failures:
    - python: nightly

install:
  - pip install -r server/requirements.txt

script:
  - cd server && python manage.py test